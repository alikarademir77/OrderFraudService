spring:
  application:
    name: order-fraud-service

---
### DEVELOPMENT ###

spring:
  profiles: dev
  cloud:
    config:
      enabled: false

management.security.enabled: false

---
### CI ###

spring:
  profiles: ci
  cloud:
    config:
      enabled: false
      uri: http://config-service-ecomm-orders-team.dev-ocp.ca.bestbuy.com
      label: master

management.security.enabled: false

---
### PRODUCTION ###

spring:
  profiles: production
  cloud:
    config:
      enabled: false # TODO - Change once we have production details
      uri: ~ # TODO - Change once we have production details
      label: master